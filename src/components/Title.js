import React from 'react';

class Title extends React.Component {
    render() {
        return (
            <h2>An earthquake has destroyed the network... help me restore it back!</h2>
        );
    }
}

export default Title;